<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Leave Requests - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <h1 class="logo">HR Management System</h1>
            <div class="nav-links">
                <a href="/">Home</a>
            </div>
        </div>
    </nav>

    <main class="main-content">
        <div class="container">
            <h1>All Leave Requests</h1>
            
            <% if (success) { %>
                <div class="success-message"><%= success %></div>
            <% } %>
            
            <% if (error) { %>
                <div class="error-message"><%= error %></div>
            <% } %>
            
            <div class="button-group">
                <a href="/leaveRequest/add" class="btn-primary">Add New Leave Request</a>
                <a href="/" class="btn-secondary">Back to Home</a>
            </div>
            
            <% if (leaveRequests.length === 0) { %>
                <p class="no-data">No leave requests found.</p>
            <% } else { %>
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>Leave ID</th>
                            <th>Employee</th>
                            <th>Leave Type</th>
                            <th>Start Date</th>
                            <th>End Date</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% leaveRequests.forEach(leaveRequest => { %>
                            <tr>
                                <td><%= leaveRequest.LeaveID %></td>
                                <td><%= leaveRequest.EmployeeName %> (ID: <%= leaveRequest.EmployeeID %>)</td>
                                <td><%= leaveRequest.LeaveType %></td>
                                <td><%= leaveRequest.StartDate.toISOString().split('T')[0] %></td>
                                <td><%= leaveRequest.EndDate.toISOString().split('T')[0] %></td>
                                <td><span class="status-badge status-<%= leaveRequest.Status.toLowerCase() %>"><%= leaveRequest.Status %></span></td>
                                <td class="action-buttons">
                                    <a href="/leaveRequest/details/<%= leaveRequest.LeaveID %>" class="btn-view">View</a>
                                    <a href="/leaveRequest/edit/<%= leaveRequest.LeaveID %>" class="btn-edit">Edit</a>
                                    <a href="/leaveRequest/delete/<%= leaveRequest.LeaveID %>" class="btn-delete">Delete</a>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } %>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 HR Management System</p>
        </div>
    </footer>
</body>
</html>
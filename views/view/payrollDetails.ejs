<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payroll Details - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Payroll Record Details</h1>
        
        <div class="details-card">
            <div class="detail-row">
                <span class="detail-label">Payroll ID:</span>
                <span class="detail-value"><%= payroll.PayrollID %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Employee:</span>
                <span class="detail-value"><%= payroll.EmployeeName %> (ID: <%= payroll.EmployeeID %>)</span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Pay Period Start:</span>
                <span class="detail-value"><%= payroll.PayPeriodStartDate.toISOString().split('T')[0] %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Pay Period End:</span>
                <span class="detail-value"><%= payroll.PayPeriodEndDate.toISOString().split('T')[0] %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Gross Pay:</span>
                <span class="detail-value">Rs. <%= payroll.GrossPay.toLocaleString() %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Deductions:</span>
                <span class="detail-value">Rs. <%= payroll.Deductions.toLocaleString() %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Net Pay:</span>
                <span class="detail-value"><strong>Rs. <%= payroll.NetPay.toLocaleString() %></strong></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Date Paid:</span>
                <span class="detail-value"><%= payroll.DatePaid.toISOString().split('T')[0] %></span>
            </div>
        </div>
        
        <div class="button-group">
            <a href="/payroll/<%= payroll.PayrollID %>/edit" class="btn-primary">Edit</a>
            <a href="/payroll/<%= payroll.PayrollID %>/delete" class="btn-delete">Delete</a>
            <a href="/payroll" class="btn-secondary">Back to List</a>
        </div>
    </div>
</body>
</html>
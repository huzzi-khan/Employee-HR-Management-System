<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Attendance Details - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <h1 class="logo">HR Management System</h1>
            <div class="nav-links">
                <a href="/">Home</a>
            </div>
        </div>
    </nav>

    <main class="main-content">
        <div class="container">
            <h1>Attendance Record Details</h1>
            
            <div class="details-card">
                <div class="detail-row">
                    <span class="detail-label">Attendance ID:</span>
                    <span class="detail-value"><%= attendance.AttendanceID %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Employee:</span>
                    <span class="detail-value"><%= attendance.EmployeeName %> (ID: <%= attendance.EmployeeID %>)</span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Work Date:</span>
                    <span class="detail-value"><%= attendance.WorkDate.toISOString().split('T')[0] %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Time In:</span>
                    <span class="detail-value"><%= attendance.TimeIn %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Time Out:</span>
                    <span class="detail-value"><%= attendance.TimeOut || 'Not Checked Out' %></span>
                </div>
            </div>
            
            <div class="button-group">
                <a href="/attendance/edit/<%= attendance.AttendanceID %>" class="btn-primary">Edit</a>
                <a href="/attendance/delete/<%= attendance.AttendanceID %>" class="btn-delete">Delete</a>
                <a href="/attendance/view" class="btn-secondary">Back to List</a>
            </div>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 HR Management System</p>
        </div>
    </footer>
</body>
</html>
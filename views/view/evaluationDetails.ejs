<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Evaluation Details - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Performance Evaluation Details</h1>
        
        <div class="details-card">
            <div class="detail-row">
                <span class="detail-label">Evaluation ID:</span>
                <span class="detail-value"><%= evaluation.EvaluationID %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Employee:</span>
                <span class="detail-value"><%= evaluation.EmployeeName %> (ID: <%= evaluation.EmployeeID %>)</span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Reviewer:</span>
                <span class="detail-value"><%= evaluation.ReviewerName %> (ID: <%= evaluation.ReviewerID %>)</span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Evaluation Date:</span>
                <span class="detail-value"><%= evaluation.EvaluationDate.toISOString().split('T')[0] %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Rating:</span>
                <span class="detail-value"><strong><%= evaluation.Rating %></strong> / 5.0</span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Comments:</span>
                <span class="detail-value"><%= evaluation.Comments || 'No comments provided' %></span>
            </div>
        </div>
        
        <div class="button-group">
            <a href="/evaluation/<%= evaluation.EvaluationID %>/edit" class="btn-primary">Edit</a>
            <a href="/evaluation/<%= evaluation.EvaluationID %>/delete" class="btn-delete">Delete</a>
            <a href="/evaluation" class="btn-secondary">Back to List</a>
        </div>
    </div>
</body>
</html>

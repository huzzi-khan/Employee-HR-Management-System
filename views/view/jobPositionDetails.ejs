<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Job Position Details - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Job Position Details</h1>
        
        <div class="details-card">
            <div class="detail-row">
                <span class="detail-label">Job ID:</span>
                <span class="detail-value"><%= jobPosition.JobID %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Job Title:</span>
                <span class="detail-value"><%= jobPosition.JobTitle %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Job Description:</span>
                <span class="detail-value"><%= jobPosition.JobDescription || 'N/A' %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Minimum Salary:</span>
                <span class="detail-value">Rs. <%= jobPosition.MinSalary.toLocaleString() %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Maximum Salary:</span>
                <span class="detail-value">Rs. <%= jobPosition.MaxSalary.toLocaleString() %></span>
            </div>
        </div>
        
        <div class="button-group">
            <a href="/job-positions/<%= jobPosition.JobID %>/edit" class="btn-primary">Edit</a>
            <a href="/job-positions/<%= jobPosition.JobID %>/delete" class="btn-delete">Delete</a>
            <a href="/job-positions" class="btn-secondary">Back to List</a>
        </div>
    </div>
</body>
</html>
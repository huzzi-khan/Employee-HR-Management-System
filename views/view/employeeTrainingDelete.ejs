<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Employee Training - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Delete Employee Training Record</h1>
        
        <div class="warning-message">
            <strong>Warning:</strong> Are you sure you want to delete this employee training record? This action cannot be undone.
        </div>
        
        <div class="details-card">
            <div class="detail-row">
                <span class="detail-label">Employee:</span>
                <span class="detail-value"><%= employeeTraining.EmployeeName %> (ID: <%= employeeTraining.EmployeeID %>)</span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Training Session:</span>
                <span class="detail-value"><%= employeeTraining.SessionTitle %> (ID: <%= employeeTraining.TrainingID %>)</span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Completion Date:</span>
                <span class="detail-value"><%= employeeTraining.CompletionDate ? employeeTraining.CompletionDate.toISOString().split('T')[0] : 'Not Completed' %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Grade:</span>
                <span class="detail-value"><%= employeeTraining.Grade || 'N/A' %></span>
            </div>
        </div>
        
        <form action="/employeeTraining/<%= employeeTraining.EmployeeID %>/<%= employeeTraining.TrainingID %>/delete" method="POST">
            <div class="button-group">
                <button type="submit" class="btn-delete">Confirm Delete</button>
                <a href="/employeeTraining/<%= employeeTraining.EmployeeID %>/<%= employeeTraining.TrainingID %>" class="btn-secondary">Cancel</a>
                <a href="/employeeTraining" class="btn-secondary">Back to List</a>
            </div>
        </form>
    </div>
</body>
</html>

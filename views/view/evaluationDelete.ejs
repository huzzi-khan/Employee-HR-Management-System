<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Evaluation - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Delete Performance Evaluation</h1>
        
        <div class="warning-message">
            <strong>Warning:</strong> Are you sure you want to delete this performance evaluation? This action cannot be undone.
        </div>
        
        <div class="details-card">
            <div class="detail-row">
                <span class="detail-label">Evaluation ID:</span>
                <span class="detail-value"><%= evaluation.EvaluationID %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Employee:</span>
                <span class="detail-value"><%= evaluation.EmployeeName %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Reviewer:</span>
                <span class="detail-value"><%= evaluation.ReviewerName %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Evaluation Date:</span>
                <span class="detail-value"><%= evaluation.EvaluationDate.toISOString().split('T')[0] %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Rating:</span>
                <span class="detail-value"><%= evaluation.Rating %> / 5.0</span>
            </div>
        </div>
        
        <form action="/evaluations/<%= evaluation.EvaluationID %>/delete" method="POST">
            <div class="button-group">
                <button type="submit" class="btn-delete">Confirm Delete</button>
                <a href="/evaluations/<%= evaluation.EvaluationID %>" class="btn-secondary">Cancel</a>
                <a href="/evaluations" class="btn-secondary">Back to List</a>
            </div>
        </form>
    </div>
</body>
</html>
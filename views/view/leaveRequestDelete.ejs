<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Leave Request - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <h1 class="logo">HR Management System</h1>
            <div class="nav-links">
                <a href="/">Home</a>
            </div>
        </div>
    </nav>

    <main class="main-content">
        <div class="container">
            <h1>Delete Leave Request</h1>
            
            <div class="warning-message">
                <strong>Warning:</strong> Are you sure you want to delete this leave request? This action cannot be undone.
            </div>
            
            <div class="details-card">
                <div class="detail-row">
                    <span class="detail-label">Leave ID:</span>
                    <span class="detail-value"><%= leaveRequest.LeaveID %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Employee:</span>
                    <span class="detail-value"><%= leaveRequest.EmployeeName %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Leave Type:</span>
                    <span class="detail-value"><%= leaveRequest.LeaveType %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Period:</span>
                    <span class="detail-value"><%= leaveRequest.StartDate.toISOString().split('T')[0] %> to <%= leaveRequest.EndDate.toISOString().split('T')[0] %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Status:</span>
                    <span class="detail-value"><span class="status-badge status-<%= leaveRequest.Status.toLowerCase() %>"><%= leaveRequest.Status %></span></span>
                </div>
            </div>
            
            <form action="/leaveRequest/delete/<%= leaveRequest.LeaveID %>" method="POST">
                <div class="button-group">
                    <button type="submit" class="btn-delete">Confirm Delete</button>
                    <a href="/leaveRequest/details/<%= leaveRequest.LeaveID %>" class="btn-secondary">Cancel</a>
                    <a href="/leaveRequest/view" class="btn-secondary">Back to List</a>
                </div>
            </form>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 HR Management System</p>
        </div>
    </footer>
</body>
</html>
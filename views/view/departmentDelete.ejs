<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Department - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Delete Department</h1>
        
        <div class="warning-message">
            <strong>Warning:</strong> Are you sure you want to delete this department? This action cannot be undone.
        </div>
        
        <div class="details-card">
            <div class="detail-row">
                <span class="detail-label">Department ID:</span>
                <span class="detail-value"><%= department.DeptID %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Department Name:</span>
                <span class="detail-value"><%= department.DeptName %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Location:</span>
                <span class="detail-value"><%= department.Location || 'N/A' %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Manager:</span>
                <span class="detail-value"><%= department.ManagerName || 'No Manager' %></span>
            </div>
        </div>
        
        <form action="/departments/<%= department.DeptID %>/delete" method="POST">
            <div class="button-group">
                <button type="submit" class="btn-delete">Confirm Delete</button>
                <a href="/departments/<%= department.DeptID %>" class="btn-secondary">Cancel</a>
                <a href="/departments" class="btn-secondary">Back to List</a>
            </div>
        </form>
    </div>
</body>
</html>
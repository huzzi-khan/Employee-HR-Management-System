<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Details - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Employee Details</h1>
        
        <div class="details-card">
            <h2>Personal Information</h2>
            <div class="detail-row">
                <span class="detail-label">Employee ID:</span>
                <span class="detail-value"><%= employee.EmployeeID %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Full Name:</span>
                <span class="detail-value"><%= employee.FirstName %> <%= employee.LastName %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">CNIC:</span>
                <span class="detail-value"><%= employee.CNIC %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Date of Birth:</span>
                <span class="detail-value"><%= employee.DateOfBirth.toISOString().split('T')[0] %></span>
            </div>
            
            <h2>Contact Information</h2>
            <div class="detail-row">
                <span class="detail-label">Email:</span>
                <span class="detail-value"><%= employee.Email %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Phone Number:</span>
                <span class="detail-value"><%= employee.PhoneNumber %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Address:</span>
                <span class="detail-value"><%= employee.Address %></span>
            </div>
            
            <h2>Employment Information</h2>
            <div class="detail-row">
                <span class="detail-label">Join Date:</span>
                <span class="detail-value"><%= employee.JoinDate.toISOString().split('T')[0] %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Status:</span>
                <span class="detail-value"><span class="status-badge status-<%= employee.Status.toLowerCase().replace(' ', '-') %>"><%= employee.Status %></span></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Job Position:</span>
                <span class="detail-value"><%= employee.JobTitle %> (ID: <%= employee.JobID %>)</span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Department:</span>
                <span class="detail-value"><%= employee.DeptName %> (ID: <%= employee.DeptID %>)</span>
            </div>
        </div>
        
        <div class="button-group">
            <a href="/employees/<%= employee.EmployeeID %>/edit" class="btn-primary">Edit</a>
            <a href="/employees/<%= employee.EmployeeID %>/delete" class="btn-delete">Delete</a>
            <a href="/employees" class="btn-secondary">Back to List</a>
        </div>
    </div>
</body>
</html>
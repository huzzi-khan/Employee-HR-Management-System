<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Attendance - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Delete Attendance Record</h1>
        
        <div class="warning-message">
            <strong>Warning:</strong> Are you sure you want to delete this attendance record? This action cannot be undone.
        </div>
        
        <div class="details-card">
            <div class="detail-row">
                <span class="detail-label">Attendance ID:</span>
                <span class="detail-value"><%= attendance.AttendanceID %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Employee:</span>
                <span class="detail-value"><%= attendance.EmployeeName %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Work Date:</span>
                <span class="detail-value"><%= attendance.WorkDate.toISOString().split('T')[0] %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Time:</span>
                <span class="detail-value"><%= attendance.TimeIn %> - <%= attendance.TimeOut || 'Not Checked Out' %></span>
            </div>
        </div>
        
        <form action="/attendance/<%= attendance.AttendanceID %>/delete" method="POST">
            <div class="button-group">
                <button type="submit" class="btn-delete">Confirm Delete</button>
                <a href="/attendance/<%= attendance.AttendanceID %>" class="btn-secondary">Cancel</a>
                <a href="/attendance" class="btn-secondary">Back to List</a>
            </div>
        </form>
    </div>
</body>
</html>
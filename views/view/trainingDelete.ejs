<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Training Session - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Delete Training Session</h1>
        
        <div class="warning-message">
            <strong>Warning:</strong> Are you sure you want to delete this training session? This will also delete all employee training records associated with this session. This action cannot be undone.
        </div>
        
        <div class="details-card">
            <div class="detail-row">
                <span class="detail-label">Training ID:</span>
                <span class="detail-value"><%= training.TrainingID %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Session Title:</span>
                <span class="detail-value"><%= training.SessionTitle %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Instructor:</span>
                <span class="detail-value"><%= training.Instructor %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Session Date:</span>
                <span class="detail-value"><%= training.SessionDate.toISOString().split('T')[0] %></span>
            </div>
        </div>
        
        <form action="/trainings/<%= training.TrainingID %>/delete" method="POST">
            <div class="button-group">
                <button type="submit" class="btn-delete">Confirm Delete</button>
                <a href="/trainings/<%= training.TrainingID %>" class="btn-secondary">Cancel</a>
                <a href="/trainings" class="btn-secondary">Back to List</a>
            </div>
        </form>
    </div>
</body>
</html>
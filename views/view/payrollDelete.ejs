<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Payroll Record - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <h1 class="logo">HR Management System</h1>
            <div class="nav-links">
                <a href="/">Home</a>
            </div>
        </div>
    </nav>

    <main class="main-content">
        <div class="container">
            <h1>Delete Payroll Record</h1>
            
            <div class="warning-message">
                <strong>Warning:</strong> Are you sure you want to delete this payroll record? This action cannot be undone.
            </div>
            
            <div class="details-card">
                <div class="detail-row">
                    <span class="detail-label">Payroll ID:</span>
                    <span class="detail-value"><%= payroll.PayrollID %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Employee:</span>
                    <span class="detail-value"><%= payroll.EmployeeName %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Pay Period:</span>
                    <span class="detail-value"><%= payroll.PayPeriodStartDate.toISOString().split('T')[0] %> to <%= payroll.PayPeriodEndDate.toISOString().split('T')[0] %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Net Pay:</span>
                    <span class="detail-value">Rs. <%= payroll.NetPay.toLocaleString() %></span>
                </div>
            </div>
            
            <form action="/payroll/delete/<%= payroll.PayrollID %>" method="POST">
                <div class="button-group">
                    <button type="submit" class="btn-delete">Confirm Delete</button>
                    <a href="/payroll/details/<%= payroll.PayrollID %>" class="btn-secondary">Cancel</a>
                    <a href="/payroll/view" class="btn-secondary">Back to List</a>
                </div>
            </form>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 HR Management System</p>
        </div>
    </footer>
</body>
</html>
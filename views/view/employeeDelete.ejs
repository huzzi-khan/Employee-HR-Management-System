<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Employee - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <h1 class="logo">HR Management System</h1>
            <div class="nav-links">
                <a href="/">Home</a>
            </div>
        </div>
    </nav>

    <main class="main-content">
        <div class="container">
            <h1>Delete Employee</h1>
            
            <div class="warning-message">
                <strong>Warning:</strong> Are you sure you want to delete this employee? This will also delete all related records (attendance, leave requests, payroll, evaluations, training records). This action cannot be undone.
            </div>
            
            <div class="details-card">
                <div class="detail-row">
                    <span class="detail-label">Employee ID:</span>
                    <span class="detail-value"><%= employee.EmployeeID %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Name:</span>
                    <span class="detail-value"><%= employee.FirstName %> <%= employee.LastName %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Email:</span>
                    <span class="detail-value"><%= employee.Email %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Department:</span>
                    <span class="detail-value"><%= employee.DeptName %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Job Position:</span>
                    <span class="detail-value"><%= employee.JobTitle %></span>
                </div>
            </div>
            
            <form action="/employee/delete/<%= employee.EmployeeID %>" method="POST">
                <div class="button-group">
                    <button type="submit" class="btn-delete">Confirm Delete</button>
                    <a href="/employee/details/<%= employee.EmployeeID %>" class="btn-secondary">Cancel</a>
                    <a href="/employee/view" class="btn-secondary">Back to List</a>
                </div>
            </form>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 HR Management System</p>
        </div>
    </footer>
</body>
</html>
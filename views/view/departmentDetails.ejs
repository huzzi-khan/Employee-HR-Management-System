<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Department Details - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <h1 class="logo">HR Management System</h1>
            <div class="nav-links">
                <a href="/">Home</a>
            </div>
        </div>
    </nav>

    <main class="main-content">
        <div class="container">
            <h1>Department Details</h1>
            
            <div class="details-card">
                <div class="detail-row">
                    <span class="detail-label">Department ID:</span>
                    <span class="detail-value"><%= department.DeptID %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Department Name:</span>
                    <span class="detail-value"><%= department.DeptName %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Location:</span>
                    <span class="detail-value"><%= department.Location || 'N/A' %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Manager:</span>
                    <span class="detail-value">
                        <% if (department.ManagerID) { %>
                            <%= department.ManagerName %> (ID: <%= department.ManagerID %>)
                        <% } else { %>
                            No Manager Assigned
                        <% } %>
                    </span>
                </div>
            </div>
            
            <div class="button-group">
                <a href="/department/edit/<%= department.DeptID %>" class="btn-primary">Edit</a>
                <a href="/department/delete/<%= department.DeptID %>" class="btn-delete">Delete</a>
                <a href="/department/view" class="btn-secondary">Back to List</a>
            </div>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 HR Management System</p>
        </div>
    </footer>
</body>
</html>

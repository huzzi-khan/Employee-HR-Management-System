<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Department Details - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Department Details</h1>
        
        <div class="details-card">
            <div class="detail-row">
                <span class="detail-label">Department ID:</span>
                <span class="detail-value"><%= department.DeptID %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Department Name:</span>
                <span class="detail-value"><%= department.DeptName %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Location:</span>
                <span class="detail-value"><%= department.Location || 'N/A' %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Manager:</span>
                <span class="detail-value">
                    <% if (department.ManagerID) { %>
                        <%= department.ManagerName %> (ID: <%= department.ManagerID %>)
                    <% } else { %>
                        No Manager Assigned
                    <% } %>
                </span>
            </div>
        </div>
        
        <div class="button-group">
            <a href="/departments/<%= department.DeptID %>/edit" class="btn-primary">Edit</a>
            <a href="/departments/<%= department.DeptID %>/delete" class="btn-delete">Delete</a>
            <a href="/departments" class="btn-secondary">Back to List</a>
        </div>
    </div>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Training Session Details - HR Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Training Session Details</h1>
        
        <div class="details-card">
            <div class="detail-row">
                <span class="detail-label">Training ID:</span>
                <span class="detail-value"><%= training.TrainingID %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Session Title:</span>
                <span class="detail-value"><%= training.SessionTitle %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Description:</span>
                <span class="detail-value"><%= training.Description || 'N/A' %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Instructor:</span>
                <span class="detail-value"><%= training.Instructor %></span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Session Date:</span>
                <span class="detail-value"><%= training.SessionDate.toISOString().split('T')[0] %></span>
            </div>
        </div>
        
        <div class="button-group">
            <a href="/trainings/<%= training.TrainingID %>/edit" class="btn-primary">Edit</a>
            <a href="/trainings/<%= training.TrainingID %>/delete" class="btn-delete">Delete</a>
            <a href="/trainings" class="btn-secondary">Back to List</a>
        </div>
    </div>
</body>
</html>